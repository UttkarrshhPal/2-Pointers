#include<iostream>
using namespace std;
int main()
{
    int height[]={1,8,6,2,5,4,8,3,7};
    int n = 9;
    //while(first<last)
    // first  = 1
    // last   = 7
    //min(first,last)=1
    // capacity = min(first,last)*widht
    // capacity = 8*1=8

    // Now height should rise
    // if(height[first]>height[last])
    // last-=1;

    // if(height[first]<height[last])
    // first+=1;

    
}